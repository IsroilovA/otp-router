# Local development only. Matches apps/server/compose.yaml.
DATABASE_URL=postgres://otp_router:local-development-only@127.0.0.1:54329/otp_router
# Compose runs the combined API/worker with the fake provider by default.
# Generate the required secrets using docs/running.md before starting it.
# OTP_ROUTER_CONFIG_DIR=../../examples/config
# OTP_ROUTER_CONFIG_FILE=router.config.ts
# OTP_ROUTER_PORT=3000
# OTP_ROUTER_POSTGRES_PORT=54329
